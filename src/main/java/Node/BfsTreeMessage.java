package Node;

public final class BfsTreeMessage extends NodeMessage {

    public BfsTreeMessage() {
        super();
    }

    public BfsTreeMessage(int sourceUID, int targetUID) {
        super(sourceUID, targetUID);
    }
}